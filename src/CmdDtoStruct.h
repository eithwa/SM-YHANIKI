#pragma once
#include <string>

namespace Yhaniki {

    /*typedef struct CmdDtoStruct {

        enum class Type {
            Nop,
            Ping, PingR,
            Hello,
            GameStartRequest,
            GameOver,
            Chat,
            SelectSong,
            LeaveLobby,
            AskSong,
            UpdateStats,
            UpdateStyle,
            UpdateOptions,
            UpdatePercentage,
            UpdateGraph,
            UpdateHasSong,
            StartFileTransfer
        };

        typedef struct BodyStruct {
            std::string content;
        } Body;

        Type cmdType = Type::Nop;
        Body cmdBody = { "" };

    } CmdDto;*/
}
